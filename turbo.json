{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],

  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".expo/**", ".output/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}

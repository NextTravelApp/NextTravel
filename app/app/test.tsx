import AnimatedIcon from "@/components/svg/AnimatedIcon";
import { View } from "react-native";

const Test = () => {
  return (
    <View className="flex w-full flex-1 items-center justify-center">
      <AnimatedIcon />
    </View>
  );
};

export default Test;
